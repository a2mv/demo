spring:
  datasource:
    url: jdbc:mysql://localhost:3306/califi?useSSL=false
    username: califi
    password: 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming: 
        strategy: org.hibernate.cfg.ImprovednamingStrategy
    properties:
      hibernate:
        diale: org.hibernate.dialect.MySQL5Dialect
server:
  context-path: /califi